package controller;

/**
 * Represents a set of commands available to use for an image processor's
 * controller, specifically with a GUI view.
 */
public interface CommandControllerGUI {

  /**
   * Takes in a specific command and performs the appropriate operations for an
   * image processing controller.
   */
  public void imageCommand();
}
